{
  "name": "secure-api",
  "version": "1.0.0",
  "description": "This API sets up a user model, generates authentication tokens, and hashes passwords to secure user data, and it uses mongoose to store the To-Do tasks of users in mongoDB.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "10.6.0"
  },
  "author": "Hussein Fardous",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.3.0",
    "lodash": "^4.17.10",
    "mongoose": "^5.2.8",
    "validator": "^10.6.0"
  }
}
